<Window x:Class="Sah.Claude.Usage.Wpf.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Settings" Height="300" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource AppBackground}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" VerticalAlignment="Top">
            <TextBlock Text="Theme"
                       Margin="0,0,0,4"
                       Foreground="{DynamicResource AppForeground}"/>
            <ComboBox x:Name="ThemeComboBox"
                      AutomationProperties.AutomationId="ThemeComboBox"
                      Width="160" HorizontalAlignment="Left"
                      SelectionChanged="OnThemeSelectionChanged"/>
        </StackPanel>

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button AutomationProperties.AutomationId="CancelButton"
                    Content="Cancel" Width="75" Margin="0,0,8,0" Click="OnCancelClick"/>
            <Button AutomationProperties.AutomationId="SaveButton"
                    Content="Save" Width="75" Click="OnSaveClick"/>
        </StackPanel>
    </Grid>
</Window>
